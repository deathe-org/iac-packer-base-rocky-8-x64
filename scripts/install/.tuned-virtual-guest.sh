#!/bin/bash -e

# Installed with anaconda/kickstart

# /bin/echo '--> Installing tuned (virtual-guest).'
# /bin/dnf --debuglevel=1 -y install \
#   tuned
# /bin/systemctl enable tuned
# /usr/sbin/tuned-adm profile \
#   virtual-guest
